<UserControl x:Class="HicoreEquipmentStudio.View.Variable.VariableMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HicoreEquipmentStudio.View.Variable"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border BorderBrush="#D9D9D9"
            BorderThickness="1"
            Background="White">

        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="45"/>
                <RowDefinition Height="250"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0"
                    Background="#FAFAFA"
                    BorderBrush="#E5E5E5"
                    BorderThickness="0,0,0,1">

                <TextBlock Text="Variables"
                           Margin="12,0"
                           VerticalAlignment="Center"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="#2F75B5"/>
            </Border>

            <!-- Toolbar -->
            <local:VariableToolbarControl Grid.Row="1"/>

            <!-- Variable Grid -->
            <local:VariableGridControl Grid.Row="2"/>

            <!-- Bottom Area -->
            <Grid Grid.Row="3" Margin="10">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <local:VariableDetailsControl Grid.Column="0"/>

                <local:VariableTestResultControl Grid.Column="1" Margin="10,0,0,0"/>

            </Grid>

        </Grid>

    </Border>
</UserControl>
