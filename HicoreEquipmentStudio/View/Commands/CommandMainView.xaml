<UserControl x:Class="HiCore.EquipmentFramework.Config.View.Commands.CommandMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cmd="clr-namespace:HiCore.EquipmentFramework.Config.View.Commands">

    <Border BorderBrush="#D9D9D9"
            BorderThickness="1"
            Background="White">

        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="38"/>
                <RowDefinition Height="200"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="#FAFAFA"
                    BorderBrush="#E5E5E5"
                    BorderThickness="0,0,0,1">

                <TextBlock Text="Commands"
                           Margin="12,0"
                           VerticalAlignment="Center"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="#2F75B5"/>

            </Border>

            <cmd:CommandToolbarControl Grid.Row="1"/>

            <cmd:CommandGridControl Grid.Row="2"/>

            <Grid Grid.Row="3" Margin="8">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <cmd:CommandDetailsControl Grid.Column="0"/>

                <cmd:CommandPreviewControl Grid.Column="1"
                                           Margin="8,0,0,0"/>

            </Grid>

        </Grid>

    </Border>

</UserControl>