<UserControl x:Class="HicoreEquipmentStudio.View.Recipes.RecipeMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HicoreEquipmentStudio.View.Recipes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="160"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
            Style="{StaticResource SectionHeaderBorderStyle}">

            <TextBlock Text="Recipes"
                   Style="{StaticResource SectionHeaderTextStyle}"/>

        </Border>

        <local:RecipeToolbarControl Grid.Row="1"/>

        <local:RecipeGridControl Grid.Row="2"/>

        <Grid Grid.Row="3"
          Margin="10,0,10,10">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <local:RecipeDetailsControl Grid.Column="0"/>

            <Grid Grid.Column="1">

                <!--<Grid.RowDefinitions>
                    <RowDefinition Height="2*"/>
                    <RowDefinition Height="2*"/>
                </Grid.RowDefinitions>-->

                <!--<local:RecipeParameterPreviewControl Grid.Row="0"/>-->

                <local:RecipeTestPreviewControl/>

            </Grid>

        </Grid>

    </Grid>
</UserControl>
