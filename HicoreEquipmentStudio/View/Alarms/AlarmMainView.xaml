<UserControl x:Class="HicoreEquipmentStudio.View.Alarms.AlarmMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:HicoreEquipmentStudio.View.Alarms">

    <Border BorderBrush="#D9D9D9"
            BorderThickness="1"
            Background="White">

        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="45"/>
                <RowDefinition Height="200"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0"
                    Background="#FAFAFA"
                    BorderBrush="#E5E5E5"
                    BorderThickness="0,0,0,1">

                <TextBlock Text="Alarms"
                           Margin="12,0"
                           VerticalAlignment="Center"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="#2F75B5"/>
            </Border>

            <!-- Toolbar -->
            <local:AlarmToolbarControl Grid.Row="1"/>

            <!-- Alarm Grid -->
            <local:AlarmGridControl Grid.Row="2"/>

            <!-- Bottom Area -->
            <Grid Grid.Row="3"
                  Margin="10">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <local:AlarmDetailsControl Grid.Column="0"/>

                <local:AlarmPreviewControl Grid.Column="1"
                                           Margin="10,0,0,0"/>

            </Grid>

        </Grid>

    </Border>

</UserControl>