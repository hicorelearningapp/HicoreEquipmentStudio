<UserControl x:Class="HiCore.EquipmentFramework.Config.View.Mapping.MappingMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:HiCore.EquipmentFramework.Config.View.Mapping">

    <Grid Background="#F5F6F8"
          Margin="6">

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="250"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="340"/>
        </Grid.ColumnDefinitions>

        <!-- LEFT -->

        <Border Grid.Row="0"
                Grid.Column="0"
                Margin="0,0,6,6"
                Background="White"
                BorderBrush="#D9D9D9"
                BorderThickness="1">

            <Grid>

                <Grid.RowDefinitions>
                    <RowDefinition Height="34"/>
                    <RowDefinition Height="36"/>
                    <RowDefinition Height="36"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="26"/>
                </Grid.RowDefinitions>

                <!-- HEADER -->

                <Border Background="#F7F7F7"
                        BorderBrush="#D9D9D9"
                        BorderThickness="0,0,0,1">

                    <TextBlock Text="SECS/GEM Mapping"
                               Margin="10,0"
                               VerticalAlignment="Center"
                               FontSize="13"
                               FontWeight="SemiBold"
                               Foreground="#2F75B5"/>

                </Border>

                <!-- TABS -->

                <TabControl Grid.Row="1"
                            Margin="6,4,6,0"
                            Height="30">

                    <TabItem Header="SVID (Variables)"/>
                    <TabItem Header="ALID (Alarms)"/>
                    <TabItem Header="CEID (Events)"/>
                    <TabItem Header="RCMD (Commands)"/>
                    <TabItem Header="PPID (Recipes)"/>

                </TabControl>

                <!-- TOOLBAR -->

                <local:MappingToolbarControl Grid.Row="2"/>

                <!-- GRID -->

                <local:MappingGridControl Grid.Row="3"/>

                <!-- FOOTER -->

                <Border Grid.Row="4"
                        Background="#FAFAFA"
                        BorderBrush="#D9D9D9"
                        BorderThickness="0,1,0,0">

                    <TextBlock Text="Total SVID Mappings: 8"
                               Margin="10,0"
                               VerticalAlignment="Center"
                               FontSize="12"/>

                </Border>

            </Grid>

        </Border>

        <!-- DETAILS -->

        <local:MappingDetailsControl Grid.Row="0"
                                     Grid.Column="1"
                                     Margin="0,0,0,6"/>

        <!-- BOTTOM -->

        <Grid Grid.Row="1"
              Grid.ColumnSpan="2">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="260"/>
            </Grid.ColumnDefinitions>

            <local:MappingCardsControl Grid.Column="0"/>

            <local:MappingSummaryControl Grid.Column="1"
                                         Margin="6,0,0,0"/>

        </Grid>

    </Grid>

</UserControl>